# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:
    # soapServerURL: '%env(SOAP_SERVER_URL)%'
    # origin: '%env(ORIGIN)%'
    # adjudicator: '%env(ADJUDICATOR)%'
    # entity: '%env(ENTITY)%'
    # organ: '%env(ORGAN)%'
    # powerType: '%env(POWER_TYPE)%'
    # mainActivity: '%env(MAIN_ACTIVITY)%'
    # soapUser: '%env(SOAP_USER)%'
    # wsseUser: '%env(WSSE_USER)%'
    # wssePassword: '%env(WSSE_PASSWORD)%'

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'
            - '../src/Resources/'

    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones

    Symfony\Component\Ldap\Ldap:
        arguments: ['@Symfony\Component\Ldap\Adapter\ExtLdap\Adapter']

    ldap:
        alias: Symfony\Component\Ldap\Ldap
            
    Symfony\Component\Ldap\Adapter\ExtLdap\Adapter:
        arguments:
            -   host: '%env(LDAP_HOST)%'
                port: '%env(LDAP_PORT)%'
                encryption: none
                options:
                    protocol_version: 3
                    referrals: false
    
    App\Service\ContractNotifierService:
        arguments:
            # No need to declare this two 
#            $client: '@Symfony\Contracts\HttpClient\HttpClientInterface'
#            $twig: '@twig'
            $url: '%env(SOAP_SERVER_URL)%'
            $origin: '%env(ORIGIN)%'
            $adjudicator: '%env(ADJUDICATOR)%'
            $entity: '%env(ENTITY)%'
            $organ: '%env(ORGAN)%'
            $powerType: '%env(POWER_TYPE)%'
            $mainActivity: '%env(MAIN_ACTIVITY)%'
            $soapUser: '%env(SOAP_USER)%'
            $wsseUser: '%env(WSSE_USER)%'
            $wssePassword: '%env(WSSE_PASSWORD)%'

